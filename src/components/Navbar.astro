<nav
  x-data="{ open: false }"
  class="sticky top-0 z-50 border-b border-white/10 bg-white/80 backdrop-blur-xl"
>
  <div class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
    <!-- Logo -->
    <a href="/" class="text-2xl font-black tracking-tight text-red-700"> Sinabung Jaya </a>

    <!-- Desktop Menu -->
    <div class="hidden items-center gap-8 md:flex">
      <a href="#jadwal" class="text-sm font-medium transition hover:text-red-600"> Jadwal </a>

      <a href="#agen" class="text-sm font-medium transition hover:text-red-600"> Agen </a>

      <a href="#tarif" class="text-sm font-medium transition hover:text-red-600"> Tarif </a>
    </div>

    <!-- Desktop CTA -->
    <a
      href="https://wa.me/6281370640212"
      target="_blank"
      class="hidden rounded-xl bg-red-600 px-5 py-3 text-sm font-semibold text-white shadow-lg transition hover:scale-105 hover:bg-red-700 md:inline-flex"
    >
      Booking
    </a>

    <!-- Mobile Button -->
    <button
      @click="open = !open"
      class="flex h-11 w-11 items-center justify-center rounded-xl border border-gray-200 bg-white md:hidden"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="h-6 w-6"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          x-show="!open"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"></path>

        <path
          x-show="open"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>
  </div>

  <!-- Mobile Menu -->
  <div x-show="open" x-transition class="border-t bg-white md:hidden" style="display: none;">
    <div class="space-y-2 px-6 py-6">
      <a
        @click="open = false"
        href="#jadwal"
        class="block rounded-xl px-4 py-3 transition hover:bg-gray-100"
      >
        Jadwal
      </a>

      <a
        @click="open = false"
        href="#agen"
        class="block rounded-xl px-4 py-3 transition hover:bg-gray-100"
      >
        Agen
      </a>

      <a
        @click="open = false"
        href="#tarif"
        class="block rounded-xl px-4 py-3 transition hover:bg-gray-100"
      >
        Tarif
      </a>

      <a
        href="https://wa.me/6281370640212"
        target="_blank"
        class="mt-4 flex items-center justify-center rounded-xl bg-red-600 px-5 py-4 font-semibold text-white"
      >
        Booking Sekarang
      </a>
    </div>
  </div>
</nav>
